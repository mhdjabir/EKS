<section class="w-100 p-4 text-center">
  <div class="row d-flex justify-content-center" id="cards">
    <div class="col-md-10 col-xl-8 text-center">
      <h3 class="fw-bold mb-4">Reviews</h3>
      <p class="mb-4 pb-2 mb-md-5 pb-md-0">
        See what our customers have to say about us!
      </p>
    </div>
  </div>

  <div class="row text-center">
    {{#each feedbacks}}
    <div class="col-md-4 mb-4 mb-md-0">
      <div class="card">
        <div class="card-body py-4 mt-2">
          <h5 class="font-weight-bold">{{this.fullname}}</h5>
          <p class="mb-2">
            <i class="fas fa-quote-left pe-2"></i>{{this.message}}
          </p>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
</section>
<div class="ft">
  <p class="chk"><small class="text-muted"></small></p>
  <a href="/feed" class="btn btn-primary">Add FeedBack</a>
</div>
<style>
  .ft {
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
  }

  .chk {
    margin-bottom: 10px;
  }

  .btn {
    margin-top: 20px;

    transition: background-color 0.3s ease;
  }

  .btn:hover {
    background-color: #007bff;
  }

  /* Reviews Section */
  .card {
    border: none;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
    transition: transform 0.3s ease, box-shadow 0.3s ease; /* Smooth hover transition */
    margin-bottom: 20PX;
  }

  .card-body {
    padding: 20px;
    background-color: #fff;
    border-radius: 10px;
  }

  .card:hover {
    transform: translateY(-5px); /* Lift effect on hover */
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Stronger shadow on hover */
  }

  .card h5 {
    font-weight: bold;
    font-size: 1.2rem;
    margin-bottom: 10px;
  }

  .card p {
    font-size: 1rem;
    color: #555;
  }

  .card i {
    color: #01080f; /* Quote icon color */
  }

  /* Optional: Add responsive design for smaller screens */
  @media (max-width: 767px) {
    .card {
      margin-bottom: 20px; /* Extra space between cards on mobile */
    }
  }
</style>