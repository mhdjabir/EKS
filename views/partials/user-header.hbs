<header class="header">
    <nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="/"><img src="/images/logo.jpg" alt="Admin Logo"></a>
            <div id="overlay" class="overlay" onclick="closeNav()"></div>
            <div id="mySidenav" class="sidenav">
                <span class="closebtn" onclick="closeNav()">&times;</span>
                {{#if user}}
                    <a href="/orders">Orders</a>
                {{/if}}
                <a href="#">About Us</a>
                <a href="/contact">Contact Us</a>
                <a href="#">Terms & Conditions</a>
            </div>
            <div>  
                <div class="profile">
                    {{#if user}}
                        <a href="/logout"><img src="/images/logout.png"></a>
                    {{else}}
                        <a href="/login"><img src="/images/login.png"></a>
                    {{/if}}
                </div>
                {{#if user}}
                <div class="cart">
                    <a href="/cart"><img src="/images/cart.png"></a>
                </div> 
                {{/if}}
                <div class="menu" onclick="openNav()"><img src="/images/menu.png"></div>
            </div>
        </div>
    </nav>
</header>